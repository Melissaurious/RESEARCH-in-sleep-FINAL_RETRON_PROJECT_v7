# TIER-0 REPAIR TABLE — proposed, NOT applied

**Nothing in this table has been applied.** Each row names the repair, the file it touches, the
claim it affects, whether it changes **scientific status** or only **wording/provenance**, and the
evidence that supports it. Three rows change what may be written and are marked
**STATUS-CHANGING** — those need explicit operator sign-off. The rest are wording or provenance.

Every repair is **additive** (an erratum or a new record). None edits a frozen bundle's numbers.

| # | repair | affected file(s) | affected claim | changes status or wording? | evidence |
|---|---|---|---|---|---|
| **T0-1** | Relabel Stage 3B's closure from "CLOSED at PARTIAL" to a **kill-criterion stop**, and record that Tier B's PASS branch was unreachable | `launchers/LAUNCHER_03B_catalytic_site_architecture.md` header; `analysis/stage3b_design/G2_DECOY_AUDIT_AND_K5_CLOSURE.md`; propagated in `analysis/stage3a_structural_core/closure/STAGE3C_HANDOFF.md` and commit `c0592f0`'s message (message itself not rewritable) | "Stage 3B closed at PARTIAL" | **STATUS-CHANGING** — PARTIAL in the launcher means a bounded-scope *pass on Tier B*; the track never evaluated Tier B and stopped on a fired kill criterion. Downstream text inherits a success reading | K5 recorded **TERMINALLY FIRED** (legitimate decoy pool **23**, 31 per chain copy, 49 with NCS pseudo-replicates, against **≥ 60**). Tier B holds **0 `HARD_PAIR`** chains while HIT requires *both* predicted residues in the truth set (`TRUTH_TABLE.tsv` v2.1; `G2_TIERA_EVALUATION.tsv`). Stage 3C independently repeats the inherited label ("3B stays CLOSED at PARTIAL") |
| **T0-2** | Supersede two statements inside the landed `cat3b_g2` README: "all 6 misses are adjacent-aspartate confusions inside the correct site" and "Decoy pool … 36" | `results/cat3b_g2_contract_and_thresholds/README.md` (via a new erratum file; the sealed bundle is **not** edited) | the miss characterisation and the decoy count | **wording/provenance** — both are already superseded elsewhere; the bundle text is stale | The operator ruling states the phrase *"may not be used"*; `G2_DECOY_AUDIT_AND_K5_CLOSURE.md` corrects the pool to **19** under the operator's definition (23 including controls). Two misses could never have been hits (`9NL3_A` truth is a single residue; `9Z6Z_H` truth has \|i−j\| = 1 vs SEP_MIN 75) |
| **T0-3** | Write `docs/decisions/` records for the Stage-3 operator rulings that currently exist only inside analysis markdown | new records under `docs/decisions/` (Ambiguity-5 resolution / 1RTD attribution; the miss ruling; the K5 firing; Stage-3C's X12 default) | provenance of every Stage-3 number | **wording/provenance** | `CLAUDE.md`: settled operator decisions live in `docs/decisions/` and are superseded by a new record, never silently. Stage 3B's rulings live only in `STOP_BEFORE_CALIBRATION_2026-09-18.md` and the K5 closure; Stage 3C names X12 as an open operator decision in its report only |
| **T0-4** | Report the **pooled and pair-weighted** R − T beside the component mean wherever X1 is cited | `results/embed_x1_conditional_pilot/README.md`, `RESULTS_DRAFT.md`, `THESIS_RESULT_STATUS.md`, `CLAIM_EVIDENCE_MATRIX.tsv` (C-08) in the embedding-report worktree | X1's "Outcome C" headline | **STATUS-CHANGING for X1 as reported in prose** (X1's *gate* outcome is not revised; X2 supersedes it as the stronger test) | X1's own `x1_arms.tsv` carries `test_nll_pooled`: R 1.39496 vs T 1.39562 = **−0.00066** pooled, against −0.01778 component-mean and **+0.00486** pair-weighted. The pooled figure appears in no narrative document. **Note:** X2 (`4f8550b`) aggregates token-weighted within component then bootstraps over components, and cross-fits all 1,075 components — so this repair is about X1's prose, not about X2 |
| **T0-5** | Erratum: `B-pop` and `B-model` in `embed_g2` were **inert by construction** | `results/embed_g2_frozen_baseline/README.md` (failure-mode 3), `scripts/a02_engine.py:133-136` comment | "ncRNA popularity and the retron-type label shortcut do not by themselves retrieve partners" | **STATUS-CHANGING** — the label-shortcut baseline was never evaluated, so that sentence claims a test that did not run; "beat every trivial baseline" was in practice "beat B-kmer" | Both scorers are indexed from *training* rows; no ncRNA appears in two folds (verified 0/16,458), so every held-out candidate scores a constant 0 — visible as 0.0200 at all six rungs and `mrr_expected_ties` = 0.0392 = 1/25.5 (all 50 candidates tied). Direction of error **flatters** the model; the rung-3 FAIL is unaffected |
| **T0-6** | Register the experimental panel as a project asset **with its exposure map**, not as independent validation | `data/README.md`, `docs/DATASET_REGISTRY.md`; this directory's `PANEL_LEAKAGE_MAP.tsv` | "175 experimentally characterised retrons are available as a benchmark" | **wording/provenance** (registration), but it **bounds** every future benchmark claim | `support.csv` sha256 `80b2f565…`; ncRNA FASTA sha256 `8a51cf71…`. Measured here: **175/175** rows carry a Mestre accession; **145/175** RTs are in the 501,561 exact-RT catalogue; **91/175** are in the embedding split (58 train, 18 val, 15 test); only **16/175** are fully external |
| **T0-7** | Fix two circular positive-control claims in the Stage-1 extended report | `results/dbchar_g7b_stage1_extended_report/REPORT.md` §6–§7 (via erratum) | "A positive control exists for the instrument … 98.6 %" (Ec107-like ncRNA carriage); the "≥ 2 of three lines" stratum | **STATUS-CHANGING** — one is offered as a *positive control* and cannot serve as one | PADLOC's `retron_Ec107-like` rule requires an ncRNA in practice (17 of 18 retron rules score on the ncRNA, `t22_padloc_rules.tsv`), so ~98.6 % carriage is near-guaranteed by the rule that defines the stratum. The "≥ 2 lines" stratum counts the ncRNA line alongside PADLOC, which is the same evidence twice |
| **T0-8** | Clear `human_input_audit` for the bundles the chosen publication route uses | the named bundles' `README.md` status blocks | every number intended to become a thesis/paper claim | **wording/provenance**, but **blocking** for promotion | Governance: a `REPRODUCIBLE` bundle suffices for the pipeline, but the operator's input audit is required *before a number becomes a paper or thesis claim*. Every Stage-1, Stage-2, Stage-3C and embedding bundle currently reads `human_input_audit: PENDING` |

## Summary

| category | rows |
|---|---|
| **STATUS-CHANGING (operator sign-off needed)** | **T0-1, T0-5, T0-7** — plus **T0-4**, which changes X1's *reported* effect size in prose though not its gate outcome |
| wording / provenance only | T0-2, T0-3, T0-6, T0-8 |

**Sequencing note.** T0-1 and T0-2 touch the same track and should land in one erratum. T0-6 should
land before any analysis uses the panel, because the exposure map is what bounds its use. T0-8 is
last, because it applies to whichever bundles the publication decision names.

**What is explicitly *not* proposed here:** no frozen number is recomputed, no bundle is re-run, no
gate is reopened, and no closed decision is reversed. Every repair is an additive record.
